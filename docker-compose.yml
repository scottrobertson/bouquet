services:
  bouquet:
    build: .
    ports:
      - "3000:3000"
    environment:
      # Directory for the database and backups.
      CONFIG_PATH: /data
      SYNC_CRON: ${SYNC_CRON:-0 4 * * *}
    volumes:
      - bouquet-data:/data
    restart: unless-stopped

volumes:
  bouquet-data:
